import { Header, Layout, Modal, Pagination, Table } from "@/components/saccontent";

function Landing() {
    return (
        <Layout>
            <Header />
            <Table />
            <Pagination />
            <Modal />
        </Layout>
    );
}

export default Landing;