import { Header, Layout,SigninForm} from "@/components/user";

function Landing() {
    return (
        <Layout>
            <Header />
            <SigninForm />
        </Layout>
    );
}

export default Landing;