import {LoginForm, Layout, Header} from "@/components/user";

function Landing() {
    return (
        <Layout>
            <Header />
            <LoginForm />
        </Layout>
    );
}

export default Landing;